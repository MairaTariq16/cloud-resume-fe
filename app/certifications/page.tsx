import Content from "@/components/content";
import { CERTIFICATIONS } from "@/lib/constants";

const CertificationsPages = () => {
  return <Content title={CERTIFICATIONS.title} items={CERTIFICATIONS.items} />;
};

export default CertificationsPages;
